apiVersion: apps/v1
kind: Deployment
metadata:
  name:
  labels:
spec:
  replicas:
  selector:
    matchLabels:
  template:
    metadata:
      labels:
    spec:
      containers:
      - name:
        image:
